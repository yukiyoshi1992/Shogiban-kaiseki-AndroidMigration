# -*- coding: utf-8 -*-
"""ユーザー利用シナリオ.xlsx を生成するスクリプト。

UATテストシナリオ.xlsx（個々の挙動を網羅する総合テスト的な一覧）とは別物。
こちらは「実際の利用者が撮影開始から共有まで、どういう一連の流れをたどるか」という
利用フロー単位のシナリオ集（chat.txt 2026-06-23夜の依頼）。
各シナリオは「→」でつないだ一連のステップとして書き、主要な利用パターンを
できるだけ網羅する。関連ケースIDはUATテストシナリオ.xlsxの行IDに対応。

仕様が変わったら、このスクリプトを直して再実行すれば再生成できる。
"""
import openpyxl
from openpyxl.styles import Font, PatternFill, Alignment
from openpyxl.utils import get_column_letter

HEADER = ["ID", "シナリオ名", "利用フロー", "確認のねらい", "関連テストケースID", "優先度"]

ROWS = [
    ("S-01", "ゴールデンパス（赤丸自動成功→通常対局→終局→共有）",
     "撮影 → 赤丸キャリブレーション成功 → グリッド確認OK → 対局開始 → 通常の指し手を数手 → 終局（確認ダイアログ） → 棋譜一覧 → 共有（LINE/メール）",
     "最も基本的な一連の流れが最初から最後まで通ること",
     "A-01,A-02,B-01,B-08,B-11,C-01,C-02,C-03", "最高"),
    ("S-02", "赤丸検出失敗→手動キャリブレーション→中止→クリップボードコピー",
     "撮影 → 赤丸キャリブレーション失敗 → 手動タップで4隅入力 → グリッド確認OK → 対局開始 → 対局中止（確認ダイアログ） → 棋譜一覧 → クリップボードにコピー",
     "自動検出が失敗した場合のフォールバック経路と、対局を中断したケースの共有手段（KENTO用コピー）が通ること",
     "A-05,A-07,A-08,B-10,B-11,C-01,C-02,C-04", "最高"),
    ("S-03", "赤丸検出成功も初期局面と不一致→手動キャリブレーションへ自動フォールバック",
     "撮影 → 赤丸キャリブレーション成功だが初期局面と不一致 → 同じ写真のまま手動タップへ自動フォールバック → グリッド確認OK → 対局開始 → 終局",
     "「検出はできたが認識結果がおかしい」場合に、撮り直しを要求せず同じ写真で手動補正に救済されること",
     "A-04,A-07,B-08", "高"),
    ("S-04", "赤丸マーカーの白飛び救済成功",
     "撮影（マーカー1点が強い光で白飛び） → 赤丸3点＋救済ロジックで4点目を判定 → グリッド確認OK → 対局開始 → 終局",
     "完全な4点検出ができない悪条件でも、無理なく自動キャリブレーションが通ること",
     "A-06,B-08", "中"),
    ("S-05", "連続撮影（早撮り）でのキュー処理",
     "対局開始 → 複数手を間を置かず連続撮影 → 「処理中（あと◯件待ち）」表示 → 順次処理が完了 → 終局",
     "早撮りしてもフリーズせず、すべての手が順番どおりに記録されること",
     "B-02,B-08", "高"),
    ("S-06", "認識エラーによる自動中止",
     "対局開始 → 指し手撮影で認識エラー（盤面が合法手で説明できない） → エラー音＋TTS＋トースト → 自動中止 → 棋譜一覧で中断局（KIFは保持）を確認",
     "誤った棋譜を残すより諦める方針が正しく働き、それまでの記録が失われないこと",
     "B-03,C-01,C-02", "高"),
    ("S-07", "通信エラーからの自動リトライ復旧",
     "対局開始 → 撮影直後に通信エラー発生 → 自動で最大3回再送 → 通信復旧 → 通常どおり手が記録される → 終局",
     "一時的な通信不調だけで対局が打ち切られず、自動で持ち直すこと",
     "B-04,B-08", "高"),
    ("S-08", "通信エラーがリトライ上限まで継続→自動中止",
     "対局開始 → 撮影 → 通信エラーが継続 → 3回リトライ失敗 → エラー音＋トースト → 自動中止",
     "通信障害が解消しない場合、サイレントに失敗せず気づける形で対局が終わること",
     "B-05", "中"),
    ("S-09", "読み上げOFFでの対局",
     "対局開始 → 「読み上げOFF」に切替 → 数手進める（読み上げなし、KIF記録は継続） → 終局",
     "読み上げを止めてもKIF記録・画面表示には影響がないこと",
     "B-06,B-08", "中"),
    ("S-10", "Bluetoothシャッターでの対局",
     "対局開始 → Bluetoothシャッター（音量キー）で撮影 → 数手進める → 終局",
     "オンスクリーンボタンを使わずハンズフリーに近い形で対局できること",
     "B-07,B-08", "高"),
    ("S-11", "終局時にキューが残っている場合の取りこぼし防止",
     "対局開始 → 連続撮影で未処理分を残したまま終局ボタン → 「処理中」のまま待機 → キューが空になった時点で自動的に終局完了",
     "終局を急いで押しても、撮影済みの手が記録から漏れないこと",
     "B-02,B-09", "高"),
    ("S-12", "キャリブレーション中の状態食い違い（409）からの自動復旧",
     "（サーバー側に前回対局の状態が残った状態で）撮影 → 409エラー検知 → 自動的にサーバー状態をリセット → 撮影しなおし → 対局開始 → 終局",
     "クライアントとサーバーの状態が食い違っても、人手を介さず自動復旧すること",
     "A-11,B-08", "中"),
    ("S-13", "棋譜一覧の閲覧のみ（操作を伴わない確認）",
     "キャリブレーション画面から「過去の対局」を開く → 一覧を見る → 対局を選んでKIF詳細を確認 → 一覧に戻る → キャリブレーション画面に戻る",
     "対局を増やさず一覧・詳細を見るだけの操作が問題なく行えること",
     "A-12,C-01,C-02,C-05,C-06,C-07", "低"),
    ("S-14", "［既知の課題・参考］Bluetoothシャッター接続時の誤発火",
     "対局中 → Bluetoothシャッター機器を新たに接続 → （想定外）音量キーイベントが誤発火 → 撮影 → 認識エラー → 自動中止",
     "5回目UAT課題①で調査中の既知の問題。再現した場合は新規不具合として報告せず、診断用トーストの有無やlogcatの記録をお願いしたい",
     "B-12（調査中・対応保留）", "高（調査中）"),
    ("S-15", "［未実装・参考］中断局の再開",
     "棋譜一覧から中断局を選ぶ → 「対局再開」 → 棋譜通りに盤面を再現して撮影 → キャリブレーション → 対局再開",
     "5回目UAT課題④で実現性検討中、現時点では未実装。実装後にこのシナリオを正式なテスト対象に追加する",
     "（未実装）", "（実装後に確定）"),
]

wb = openpyxl.Workbook()
ws = wb.active
ws.title = "利用シナリオ"

ws.append(HEADER)
header_fill = PatternFill(start_color="2F5496", end_color="2F5496", fill_type="solid")
header_font = Font(color="FFFFFF", bold=True)
for col_idx, _ in enumerate(HEADER, start=1):
    cell = ws.cell(row=1, column=col_idx)
    cell.fill = header_fill
    cell.font = header_font
    cell.alignment = Alignment(horizontal="center", vertical="center")

wrap = Alignment(wrap_text=True, vertical="top")
note_fill = PatternFill(start_color="FFF2CC", end_color="FFF2CC", fill_type="solid")
future_fill = PatternFill(start_color="F4CCCC", end_color="F4CCCC", fill_type="solid")

for row in ROWS:
    ws.append(row)
    r = ws.max_row
    for c in range(1, len(HEADER) + 1):
        ws.cell(row=r, column=c).alignment = wrap
    if row[0] == "S-14":
        for c in range(1, len(HEADER) + 1):
            ws.cell(row=r, column=c).fill = note_fill
    if row[0] == "S-15":
        for c in range(1, len(HEADER) + 1):
            ws.cell(row=r, column=c).fill = future_fill

widths = [6, 34, 60, 36, 22, 14]
for i, w in enumerate(widths, start=1):
    ws.column_dimensions[get_column_letter(i)].width = w

ws.freeze_panes = "A2"
ws.auto_filter.ref = f"A1:{get_column_letter(len(HEADER))}{ws.max_row}"

out_path = "ユーザー利用シナリオ.xlsx"
wb.save(out_path)
print("saved:", out_path, "rows:", len(ROWS))
