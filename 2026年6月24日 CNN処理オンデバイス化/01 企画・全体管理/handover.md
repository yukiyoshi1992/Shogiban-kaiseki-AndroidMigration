# CNN処理オンデバイス化 検証PJ 引継ぎログ

このファイルは本サブPJ（CNN処理のスマホ側オンデバイス化検証）専用のセッション間引継ぎログ。
本体プロジェクト（`\\YukiYoshiNAS\Shogiban-kaiseki-appli`）のCLAUDE.md/`01 企画・管理/handover.md`とは別管理——
本PJの作業に着手する際はこちらと`02 要件定義/要件定義.md`を読めば再開できる。

## 現在の状態（2026-06-23、初回セッション）

ユーザーが外出する直前に依頼。`01 企画・全体管理/実施したいこと.txt`を読み、
ユーザー不在のまま判断できる範囲で要件定義の一次ドラフトと、技術的な実現性調査の
最初の一歩を進めた。**要件定義.md内の「要確認」マークの項目は、ユーザー帰宅後に
回答をもらって確定させる必要がある。**

### やったこと
1. `01 企画・全体管理/実施したいこと.txt`を読み、目的（CNN処理オンデバイス化によるレスポンス改善の検証）・前提（既存コード変更禁止、複製環境で開発、明確に良ければ本番移行）を理解。
2. `02 要件定義/要件定義.md`を作成。スコープ（CNN推論＋透視変換まで／指し手判定以降はサーバー継続、要確認）、モデル変換方式の比較、画像前処理の実装方針、アプリ複製方法、比較検証手順、移行判断基準を整理。要確認事項は明記し、推奨案を添えた。
3. `03 設計・開発/01 モデル変換/`に技術検証PoCを実施・記録：
   - 既存モデル（`00 runtime/server/models/best_model.pth`、MobileNetV2）をこのフォルダにコピー（複製環境の原則、元ファイルは変更していない）。
   - `export_torchscript_poc.py`：`torch.jit.trace`でのTorchScript化に成功。`optimize_for_mobile`は数値的に大きく異なる出力を生成する不具合を発見し、不採用と判断（traceのみで保存）。
   - `verify_real_photo.py`：実際の保存済み写真でも、エクスポート後モデルが元モデルと完全一致することを確認。
   - **発見した環境課題**：このNAS共有（UNCパス＋日本語パス）に対し、PyTorchのネイティブ保存/読み込みAPI（`_save_for_lite_interpreter`・`torch.jit.load`）が直接ファイルを扱えず失敗する（`errno 42`）。ローカル一時ディレクトリ経由でコピーする回避策を両スクリプトに実装済み（CLAUDE.mdの`cv2.imread`/`imwrite`注意と同根の問題）。
   - **ExecuTorch（PyTorch公式が推奨する後継ランタイム）の検証は、このPCの環境問題でブロック中**：`pip install executorch`がWindowsの260文字パス長制限（Long Path未対応）で失敗。`import`自体はなぜか通るが`pip show`では未インストール扱いの不完全な状態のため、この状態での検証は避けた。要件定義.mdに対応案（①Windowsの長いパス設定を有効化／②WSL等の別環境で変換作業を行う／③ひとまずPyTorch Mobile Lite（非推奨だが動作確認済み）で進める）を記載、ユーザー判断待ち。

### 続き（同日、ユーザーから「進めてください」の指示を受けて着手）

要件定義.mdの「要確認」項目はまだユーザー回答待ちだが、「決めるべき要件は確認しながら進める、できるところから開発を進めてしまえるとベター」という当初の依頼に従い、各項目の推奨案をデフォルト採用してAndroid側の実装に着手した。

4. `03 設計・開発/01 Androidアプリ開発/`（本体プロジェクトの既存Androidアプリ）をフォルダ単位で`03 設計・開発/02 Androidアプリ開発/`にコピー（ビルドキャッシュ.gradle/.idea/build等は複製不要なので削除）。パッケージ名を`com.example.shogiban_kaiseki_appli_ondevice`に変更、本番アプリと共存インストールできるようにした。
5. 実装スコープは要件定義.mdの推奨案から絞り込んだ：カメラ撮影・4隅タップUI・対局フロー全体は複製せず（Claude側に実機もカメラもなくライブ操作を検証できないため）、PCで自動検出済みの実写真2枚（4隅座標つき、初期配置と完全一致確認済み）をassetsにバンドルし、「オンデバイス推論」と「サーバー推論（別ポート8001番、本番8000番のセッション状態を壊さないため）」を同条件で比較するだけの単一画面ベンチマークアプリにした。
6. `recognition/OnDeviceRecognition.kt`：サーバー側`recognition.py`のwarp_board・predict_boardをOpenCV(`org.opencv:opencv:5.0.0`)+PyTorch Mobile Lite(`org.pytorch:pytorch_android_lite:2.1.0`)で再現。
7. **新たに見つかったリスク**：`.ptl`をエクスポートしたPyTorch（2.12.0、PC側）と、Android側ライブラリの最新版（2.1.0、2023年10月公開で約2年古い——ExecuTorch移行のため更新停止）にバージョン差があり、Lite Interpreterのバイトコード形式に互換性があるか未確認（PC上の`_get_model_bytecode_version`チェックがエラーになったが、`torch.jit.load`自体は問題なく読めている）。**実機での読み込み成否はAndroid Studio・実機がないこの環境では検証不能**——要件定義.md 3節に詳細記載。
8. ビルド検証は`py_compile`相当の手段がないため、全.ktファイルの中括弧・丸括弧の対応のみ確認（コンパイルは未確認）。

### 次にやること（ユーザー対応待ち、最優先）
1. **`03 設計・開発/02 Androidアプリ開発/`をAndroid Studioで開いてGradle Sync→実機ビルド**。上記7のバージョン差リスクが実際に問題になるかはこれでしか分からない。
2. ベンチマーク比較用に`00 runtime/server`で`uvicorn main:app --host 0.0.0.0 --port 8001`を起動（既存コード無変更、本番8000番とは別プロセス）。
3. 実機で「オンデバイスで実行」「サーバーで実行（比較用）」を押し、処理時間・認識結果一致を確認。
4. 要件定義.mdの残りの「要確認」項目（検証スコープ、移行判断基準の閾値）の回答。

### 引継ぎ・運用ルール（本体プロジェクトと共通）
- 作業は1単位ごとにこのファイルと`要件定義.md`を更新し、commit & push する。
- Discordでの報告・確認も本体プロジェクトと同じ運用（`yoshi19920305`、`reply`ツール使用）。
- 既存ソース（`00 runtime/server/`・既存Androidアプリ）は変更しない、というのが本PJの最重要前提——間違っても本体側のファイルを直接編集しないこと。
