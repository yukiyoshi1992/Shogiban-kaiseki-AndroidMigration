# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working in this repository.

## What this project is

A native Android app + small FastAPI server that lets a user record a shogi (将棋) game as a KIF file by photographing the board after every move, with the board-recognition CNN doing the actual reading. This is the **successor** to a finished predecessor project, `\\YukiYoshiNAS\Shogiban-kaiseki-tool` (a Streamlit/Python desktop tool that proved the recognition pipeline works end-to-end). That predecessor is **done and archived as of 2026-06-20** — do not edit it; treat it as a read-only reference and a source of reusable code/model/lessons-learned. This repo starts at the requirements-definition stage; nothing has been built here yet (`03 設計・開発/01 Androidアプリ開発/` and `02 API開発/` are empty).

For session-to-session continuity and current status, see `01 企画・管理/handover.md` — read it alongside this file before doing anything.

## Why this project exists (carried over from `01 企画・管理/課題.txt`)

The predecessor's actual operational pain points, which this rebuild is meant to solve:
1. **No real-time-ness.** The old flow was: phone camera app → folder-sync app → NAS folder → Python watches the folder. Sync only runs every few minutes, so there's dead time after taking a photo before the system reacts, and the operator can't tell when it's safe to take the next photo.
2. **Too many apps/devices in the loop.** The phone needs a separate camera app, a separate sync app, and a separate file-manager app; when automatic detection fails, a human has to go to the *server* to do manual calibration. Sharing/handing off the finished KIF (e.g. to KENTO for game analysis) isn't integrated either.
3. **Orientation isn't fixed.** The old camera app lets photos come in portrait or landscape unpredictably, so the pipeline has to *detect* orientation (blue-triangle marker) and has a whole human-fallback path for when that detection is wrong. The plan for this project is to control the camera (build it into the native app) so orientation is fixed by construction and this detection step disappears entirely.

Resolution direction already decided (`02 要件定義/ネイティブアプリ化_要件.xlsx`): a native Android app owns the camera and the whole user flow; it talks to a small FastAPI server (on the same Windows PC that used to run the Streamlit tool) over HTTP instead of folder-sync, for real-time turnaround; the recognition logic itself is reused from the predecessor project, not rewritten.

## Architecture decisions (2026-06-20, see `02 要件定義/アーキテクチャ検討.md` for full detail)

- Predecessor's model + recognition logic (`load_model`/`predict_board`/`order_points`/`compute_calib`/`classify_frame`/`save_kif`) will be **copied into this repo** (not referenced in place) — the predecessor stays frozen/read-only, so any future fix (e.g. the known `MOVE_DIFF_THRESHOLD` weakness, see below) has to happen on the copy.
- Android stack decided: **Kotlin + Jetpack Compose + CameraX + Retrofit**, developed in **Android Studio** under `03 設計・開発/01 Androidアプリ開発/`. User is new to Android dev — confirm tooling/setup questions explicitly rather than assuming familiarity.
- FastAPI endpoint sketch: `POST /calibration/photo`, `POST /calibration/confirm`, `POST /move`, `POST /game/end`, `GET /games`, `GET /games/{id}` — single in-memory session, state machine `idle→calibrating→ready→playing→finishing→idle` (mirrors predecessor Streamlit app's state machine). **Not yet built** — only a minimal stub exists so far (see below).
- **Implementation status (2026-06-21)**: Android project created and runs successfully on a real device (OPPO) from `03 設計・開発/01 Androidアプリ開発/` (package `com.example.shogiban_kaiseki_appli`, placeholder — may get renamed). **CameraX capture → API upload is implemented AND end-to-end verified on real hardware** (`MainActivity.kt`'s `CameraPermissionGate`, `camera/CameraScreen.kt`, `network/ApiConfig.kt`/`PhotoApiService.kt`/`RetrofitClient.kt`): shutter button takes a photo via CameraX `ImageCapture` and posts it as multipart to `/photo` via Retrofit; confirmed working OPPO→FastAPI→`received_photos/`. `network/ApiConfig.kt`'s `BASE_URL` is set to the real PC LAN IP (`192.168.0.24` as of 2026-06-21 — re-check via `ipconfig` if it changes, this PC doesn't have a DHCP reservation yet).
- **Server-side recognition integration done (2026-06-21)**: `03 設計・開発/02 API開発/` now has `recognition.py` (model load/predict, 4-corner `order_points`/`compute_calib`, `classify_frame`, `move_to_text`/`save_kif` — ported from predecessor's `run_realtime.py`, model files copied into `models/`) and `session.py` (in-memory `GameState` singleton: `idle→ready→playing→finishing→idle`; note "calibrating" from the architecture doc collapsed into the idle→ready transition since the server only ever sees a request once photo+4-points are already in hand — see `session.py` docstring). `main.py` now implements the full endpoint set from the architecture doc: `POST /calibration/photo`, `POST /calibration/confirm`, `POST /move`, `POST /game/end`, `GET /games`, `GET /games/{game_id}` — verified end-to-end with a live `uvicorn` instance + `curl` (synthetic image, not a real board photo) covering the full idle→ready→playing→idle cycle including KIF file creation/append/retrieval. The old `/health`/`/photo` stub endpoints are left in place unchanged (still used by the Android app's current minimal test flow; not yet migrated to the new endpoints — that's Android-side follow-up work). **`classify_frame` threshold fix applied as planned**: only accepts a candidate move (or 2-move pair) that drives the diff to exactly 0; otherwise escalates depth or returns `"error"` — no more "good enough" `MOVE_DIFF_THRESHOLD<=2` acceptance. **Open item #6 (production photo storage) resolved**: new endpoints save into `02 API開発/runtime/photos/` and `runtime/games/` (both gitignored), separate from the old `received_photos/` test-flow folder — matches predecessor's runtime/ convention.
- **Android calibration/play screens implemented (2026-06-21, not yet Gradle-built/device-tested — that's the user's next step)**: `calibration/CalibrationScreen.kt` (camera capture → tap exactly 4 board corners on the captured photo, displayed via `BoxWithConstraints`+`onSizeChanged` so tap coordinates can be rescaled to the original full-resolution photo before sending → `POST /calibration/photo` → renders the returned 9x9 grid as kanji text via new `board/PieceDisplay.kt` for human confirmation → "OK" calls `POST /calibration/confirm` and advances, "やり直す" goes back to step 1, no per-cell correction UI per the already-confirmed no-individual-cell-editing decision) and `play/PlayScreen.kt` (continuous camera preview, shutter via on-screen button or **hardware volume-up key** hooked in `MainActivity.onKeyDown` per the architecture doc's Bluetooth-shutter plan, calls `POST /move`, TTS-speaks `speech_text` on `"move"`, silently ignores `"nochange"`, on `"error"` auto-retakes once then gives up with a `ToneGenerator` beep — matches the "retry once, then error" policy; "終局" button calls `POST /game/end`). `MainActivity.kt` now hosts a tiny in-file screen switcher (`AppScreen.Calibration`/`Playing`, no nav library needed for just 2 screens) and toggles `FLAG_KEEP_SCREEN_ON` only during `Playing` — this resolves open item #5 (WakeLock) using the simpler of the two options the architecture doc named. New `network/ShogiApiService.kt`/`ShogiApiModels.kt` mirror the server's calibration/move/end endpoints; `RetrofitClient` now exposes both `photoApiService` (old) and `shogiApiService` (new). The old `camera/CameraScreen.kt` (the `/photo`-only connectivity test screen, now unreferenced) was moved to `camera/@old/CameraScreen.kt` per this user's stated `@old/` convention rather than deleted. **Not yet verified end-to-end on a real device** — written and manually code-reviewed (no `kotlinc`/Gradle available in this non-interactive environment to compile-check; per working agreement, Gradle Sync/Run is the user's step in Android Studio). **Next increment**: user runs Gradle Sync + installs on the OPPO device, tests the full calibration→play→end flow against the running `uvicorn` server, and reports back any compile errors or UX issues (mirrors the pattern from the CameraX increment, which needed 2 rounds of compile-error fixes after the first Android Studio build attempt).
- **Calibration UX overhaul, driven by real-device testing (2026-06-21)**: first real on-device test of the calibration screen surfaced three usability problems, all now fixed:
  1. **Camera preview looked more zoomed-in than the actual capture** (board didn't appear to fit, but the captured photo actually did) — root cause: `PreviewView`'s default `scaleType` (`FILL_CENTER`) crops the preview to fill the view. Fixed by setting `PreviewView.ScaleType.FIT_CENTER` in both `CalibrationScreen`'s `CameraCaptureStep` and `PlayScreen` — user confirmed the actual captured photo already included the full board even before this fix, so this was a display-only bug.
  2. **Manual 4-corner tapping was "too hard, always off"** — fixed two ways together: (a) **red-circle auto-calibration now runs first** (see corrected note above) — `POST /calibration/photo` now accepts `points` as optional; omitting it triggers `recognition.calibrate_from_image()` (ported red-circle detection), returning `{"status": "calibration_failed", "reason": "red_circles_not_found"}` if no circles are found, at which point the Android client falls back to manual tapping on the same already-captured photo (no retake needed). (b) When manual tapping **is** needed, `TappingStep` in `CalibrationScreen.kt` was rewritten from plain-tap-to-place to **press-and-drag-with-magnifier**: pressing down places/grabs a point immediately (hit-tests existing points within a ~28dp radius for re-grabbing vs. placing a new one), dragging shows a live 160dp magnified loupe (positioned above the finger, flips below if no room) with a crosshair so the user can see exactly where the corner will land before lifting.
  3. **Human visual confirmation of the recognized 9x9 grid was redundant friction** — since calibration is always against the known standard initial position, the user said the server should just auto-judge pass/fail instead of asking a human to eyeball 81 kanji. Added `recognition.compare_to_initial()` (compares `predict_board()`'s output against `board_to_label_grid(shogi.Board())` — reuses the same initial-position source of truth `classify_frame` already relies on, rather than a separate hardcoded table like the predecessor's `INITIAL_BOARD_STD`). `/calibration/photo`'s response now includes `matches_initial`/`mismatch_count`; the Android `CalibrationScreen` auto-calls `/calibration/confirm` and proceeds straight to `Playing` on a match, or shows a mismatch-count message with only a "retake" option on a non-match (no manual "OK" override, no per-cell editing — consistent with the already-confirmed no-individual-cell-correction policy). The old human-confirmation `ConfirmStep` composable was removed entirely (not parked in `@old/` — it's a UI flow, not standalone reusable logic, and the git history already preserves it if ever needed).
  4. **Bluetooth shutter (volume key) now also works during calibration's photo step**, not just during play — `registerShutterTrigger` is now threaded into `CalibrationScreen`'s `CameraCaptureStep` too (previously only `PlayScreen` registered it). Also widened from `KEYCODE_VOLUME_UP`-only to **both volume up and down** per explicit user request.
  - **Caveat (known, not yet handled)**: if the user touches the tapping image while already 4 points are placed, the gesture handler silently no-ops without fully draining that pointer's event stream within `awaitEachGesture` — low-severity (visually harmless even if it misfires), not fixed due to time pressure during live user testing; revisit if it ever causes a visible glitch.
- **Calibration overhaul round 2, same live-test session (2026-06-21)**:
  - **Build error**: `awaitEachGesture`/`awaitFirstDown` were imported from the wrong package (`androidx.compose.ui.input.pointer`); verified via the actual Gradle-cached Compose sources that they live in **`androidx.compose.foundation.gestures`** instead (member function `awaitPointerEvent`/`PointerInputChange.consume()`/`.position` are correctly in `ui.input.pointer` and needed no import). Fixed.
  - **Auto red-circle calibration found 4 "circles" but produced a nonsense calibration** (all-81-cells-`"empty"` recognition → exactly 40 mismatches vs. the 40 occupied initial squares) — root cause not yet confirmed; suspect the verbatim-ported `detect_red_circles`'s **hardcoded absolute pixel radius bounds (`8 <= r <= 80`)** don't scale with this app's (likely much higher-resolution CameraX) photos the way they did for whatever camera the predecessor used, so real markers get filtered out and `select_board_corners`'s fallback grabs spurious reddish blobs instead. **Not yet fixed** — needs the actual failing photo (saved server-side at `02 API開発/runtime/photos/calib_*.jpg`) to diagnose concretely rather than guess; asked the user to send it.
  - **Behavior fix regardless of the above root cause**: both `"calibration_failed"` (no circles found) and a recognized-but-mismatched result now fall through to the **same manual 4-tap fallback**, matching the predecessor's actual `run_realtime.py` policy exactly ("any of {orientation/red-circle/initial-position} failing → human manually calibrates immediately, no retake-and-hope loop"). Previously mismatch dead-ended at a "please retake the photo" screen, which was wrong per this policy. Also: once a human manually taps the 4 corners, the result is **accepted unconditionally** (no second mismatch-check round) — again mirroring the predecessor's explicit design choice to trust manual human calibration outright.
  - **Volume key shutter trigger didn't fire on-device** despite registering correctly — switched `MainActivity` from overriding `onKeyDown` to overriding **`dispatchKeyEvent`** instead (fires earlier in the dispatch chain, less likely to be swallowed by Compose focus handling or OEM key interception). `onKeyDown` is removed entirely now (dispatchKeyEvent consuming the event makes it redundant).
- **Calibration overhaul round 3 (2026-06-21, long live-test session) — root cause finally confirmed, policy on touching predecessor's calibration logic changed, several things still broken at session end:**
  - **Orientation was never actually fixed.** Realized the app let the phone be held in any rotation, so captured-photo orientation varied run to run — directly undermining the whole "sente always on the left" premise (predecessor's `INITIAL_BOARD_STD`/training-data convention, and architecture decision #3's entire point). Fixed via `android:screenOrientation` in `AndroidManifest.xml`. **Picked the wrong value first** (`landscape`, based on misreading old files under `Shogiban-kaiseki-tool/runtime/input/`, which turned out to be unrelated stray landscape test shots) — user pointed at the actual accepted reference, `Shogiban-kaiseki-tool/UAT/case3/IMG_20260620_122007.jpg`, which is **portrait, 2160×3840 (16:9)**. Corrected to `android:screenOrientation="portrait"`.
  - **Aspect ratio was the other half of the same problem.** CameraX's `ImageCapture`/`Preview` default to **4:3** (confirmed: captured photos were 2448×3264) while the predecessor's camera — and this device's own stock camera app — used **16:9 (3840×2160 spec)**. The 4:3 default left noticeably more background margin around the board than the predecessor's framing, which matters because `select_board_corners` works by nearest-circle-to-each-image-corner (see below) — more margin means more room for background junk to be "closer to a corner" than the real marker. Fixed by adding a `ResolutionSelector` with `AspectRatioStrategy.RATIO_16_9_FALLBACK_AUTO_STRATEGY` to both `Preview.Builder()` and `ImageCapture.Builder()` in `CalibrationScreen.kt` and `PlayScreen.kt`. (A blind "just apply a fixed zoom ratio" fix was tried first and explicitly rejected by the user as unprincipled guessing — correctly so, since the actual cause was the aspect ratio mismatch, not field of view.)
  - **Root cause of red-circle miscalibration, nailed down with hard evidence — and the correct framing of it.** `select_board_corners` (verbatim from predecessor, in both `recognition.py` and predecessor's own `run_realtime.py`) picks, for each of the image's 4 geometric corners `(0,0),(w,0),(w,h),(0,h)`, whichever detected red-ish circle is nearest to it — which mis-selects whenever a stray reddish object sits near a frame edge (confirmed twice this session: a black gadget with a red marking, then a multicolor ball). **Decisive verification performed**: ran predecessor's own unmodified `run_realtime.py` against the exact same failing photos saved by this project's server — byte-for-byte identical wrong output, proving this is not a porting bug; the algorithm itself behaves identically either way given the same input photo.
    **Important correction to how to think about this** (user pushed back on an earlier draft of this note that framed it as "the predecessor's algorithm was always secretly bad"): the right framing is that **the predecessor's exact camera/capture settings are unknown and effectively unreproducible**, and the predecessor's setup evidently produced photos clean/tight/controlled enough (board filling the frame, no stray red objects near the corners) that this latent algorithmic fragility never got triggered in practice. This project's camera capture conditions still don't fully match whatever the predecessor's did — the 16:9 aspect-ratio fix (above) closed one known gap, but evidently not the only one, since stray-object misdetection kept recurring even after that fix. So there are two independent, both-valid angles to pursue, not just one: (a) make `select_board_corners` itself more robust (less sensitive to background clutter), and/or (b) keep narrowing the gap between this app's actual capture conditions and whatever the predecessor's camera setup was actually doing (framing tightness, distance, zoom, lighting) — we don't have the predecessor's exact settings recorded anywhere, so (b) is necessarily trial-and-error against UAT reference photos.
  - **Policy change, explicit and user-approved**: the long-standing "don't touch predecessor's red-circle/board-analysis logic" constraint is **lifted specifically for `select_board_corners`'s corner-selection heuristic** (not the HSV circle-detection step, which can stay as-is) — user's own words: "もうあきらめて赤丸キャリブレーションのコードはこのPJはこのPJで新たに作り直そう." **Not yet redesigned** — this is the top of the next-session queue. Leading idea floated but not committed: pick the 4-point combination, among all detected candidates, that forms the largest-area convex quadrilateral (closer to "this is the board's own outline" than "nearest to the image's literal corners").
  - **`main.py` photo-save bug found and fixed, then a save regression appeared with no diagnosis yet**: `_save_runtime_photo` was being called *after* the early `return` on `calibration_failed`, so the exact cases needing investigation were the ones never saved. Moved the save to happen unconditionally near the top of `/calibration/photo`. This worked for several photos (confirmed: 15:08 and 15:12 attempts saved and — once the 16:9 fix landed — actually calibrated correctly, mismatch_count=2, near-perfect). **But by the end of the session, new calibration attempts stopped saving anything at all**, even after removing the stray red object from frame. Not diagnosed — next session should check the `uvicorn` console for exceptions/stack traces, confirm the server process actually picked up the latest `main.py` (restart it), and confirm the phone can actually reach the server (the request might not be arriving at all, vs. arriving and erroring before the save line).
  - **Manual tap-then-zoom calibration UI was rewritten, twice, and is still broken at session end.** First rewrite (drag + small floating loupe) was reported as "I don't understand how to use it, touching the loupe does nothing." Second rewrite (tap rough corner → view zooms into that region → tap again precisely to confirm → zooms back out; repeat ×4) was reported as **placing the confirmed point at the wrong location — specifically, at the position the tap would have meant on the de-zoomed/overview image, not translated through the zoom**. Attempted fix: suspected a `pointerInput` key-restart timing issue (the gesture detector coroutine possibly still running with a stale closure from before zooming in) and replaced it with a `rememberUpdatedState`-based version that never restarts `pointerInput` and always reads current `crop`/`points`/`baseScale` through `State` objects. **User retested on the fresh local clone (so the fix was definitely included) and confirmed the bug still happens.** The actual root cause is still unknown — this diagnosis was wrong, or there's a second, different bug. Needs fresh investigation next session, probably by rendering on-screen debug text showing the raw tap position, the computed crop rect, and the computed bitmap position live, so the mismatch can be seen directly rather than inferred.
- **Session 5 (2026-06-21) — three of the four carried-over items addressed via static code review (no device available this session; all pending real-device retest):**
  1. **Photo-save regression (root cause found, fixed)**: in both `/calibration/photo` and `/move` (`main.py`), the `session.state` gate (`raise HTTPException(409, ...)`) ran *before* `_save_runtime_photo(...)`, not after as the prior session's fix intended — so any request arriving while the server was stuck in a non-`idle/ready` (or non-`playing`, for `/move`) state was rejected before the photo was ever written to disk. This exactly matches "calibration attempts stopped saving anything at all" if a prior test run left `session.state` at `PLAYING` without ever calling `/game/end` (the in-memory `GameState` singleton has no path back to `idle` except `/game/end` or a server restart). Fixed by moving `_save_runtime_photo` to immediately after image decode, before the state check, in both endpoints — a photo is now saved unconditionally on every call that successfully decodes an image, regardless of what happens after.
  2. **`select_board_corners` redesigned** per the plan floated at the end of the last session (and approved again this session): replaced the old "nearest detected red circle to each of the image's 4 geometric corners" heuristic with **the 4-point combination (out of all detected candidates) whose convex hull has the largest area** (`itertools.combinations` over candidate circle centers, `cv2.convexHull`/`cv2.contourArea` to score each). Rationale: the board's true corners should span the candidates' widest extent; a stray reddish background object sitting near a frame edge (the repeatedly-observed failure mode) produces a smaller quadrilateral than the true board corners in a tightly-framed (16:9, board-filling) shot, so it should lose on area rather than win on "nearest to a literal pixel corner." Only this corner-*selection* step changed — `detect_red_circles` (HSV thresholds, circularity/radius prefiltering) is untouched, per the user's explicit scope (lifted constraint applies only to corner selection).
  3. **Coordinate-mismatch bug in the manual tap-then-zoom UI: root cause found and fixed for real, via the debug overlay's actual numbers (not a guess).** Initial hand-derivation of the existing `rememberUpdatedState` version found no inconsistency on paper, so rather than attempt a third blind fix, added an on-screen debug `Text` and asked the user to reproduce and send a screenshot. The screenshot showed `boxSize=1080 x 1510` — but this `TappingStep`'s `Box` was sized via `boxHeightDp = maxWidth / aspect` specifically *to force* the box's aspect ratio to match the bitmap's (2160×3840 → portrait ~0.5625), which at `maxWidth=1080` requires a height of ~1920px, not 1510px. **The actual root cause: that height request gets clamped down by the parent `Column`'s `weight(1f)` whenever there isn't enough vertical screen room (hint/debug text + button row eating into it), so the box ends up shorter than the aspect-correct height.** Once box-aspect ≠ bitmap-aspect, Compose's `Image(... Modifier.fillMaxSize())` (default `ContentScale.Fit`) pillarboxes/letterboxes the displayed photo inside the box — but every coordinate conversion in the tapping code assumed the photo filled the box edge-to-edge with one uniform scale factor and no offset. **This exact assumption was shared by both prior UI rewrites (drag+loupe, then tap-zoom-confirm) — which is why the bug survived both redesigns: it was never in the zoom-transform math itself.** Fix: replaced the `boxHeightDp`-forcing layout with a plain `.weight(1f)` box (no forced aspect), and introduced `FitRect`/`computeFitRect()` (mirrors `ContentScale.Fit`'s own fit-and-center algorithm) as the single source of truth for box↔bitmap coordinate conversion in both zoomed and unzoomed states; `points` now stores bitmap-pixel coordinates directly (simpler than the old "box units" indirection). The debug overlay was kept in place (not removed) so the next test can directly confirm the numbers now line up. Verified against the user's own real failure screenshot's numbers by hand — the bug reproduces exactly as described once the letterboxing is accounted for.
  4. **Volume-key shutter: root cause found and fixed, via the diagnostic toast's result.** User reported the toast does fire ("音量キー検知→シャッター実行") but nothing actually happens — confirmed precisely via a follow-up question: on-screen shutter button advances `CalibrationScreen` to the next phase, the volume key does not, even though both are meant to call the same `capture()`. Root cause: in `CameraCaptureStep` (`CalibrationScreen.kt`), `imageCapture` is an **ordinary function parameter** (not a `remember`-backed reactive state — unlike `PlayScreen`, where the equivalent state lives in the same composable scope as its own `LaunchedEffect`/registration). `LaunchedEffect(Unit) { registerShutterTrigger { capture() } }` only runs once, on the very first composition — at that moment `imageCapture` is still `null` (camera binding is async). Because it's a plain parameter, the registered `capture()` closure captured *that first composition's `null` snapshot* permanently; later recompositions (after the camera finishes binding and a non-null `imageCapture` is passed down) create *new* `capture()` instances that are never re-registered, since the effect's key never changes. The on-screen `Button`'s `onClick = { capture() }` doesn't have this problem because it's rebound fresh on every recomposition the user could actually see/tap. Fixed with `rememberUpdatedState(imageCapture)` (same idiom already used in this file's `TappingStep` for an analogous registered-once-but-needs-live-values situation) so `capture()` always reads the current value regardless of when it was registered. **`PlayScreen.kt`'s equivalent volume-key wiring was structurally checked and should *not* have this bug** (its `imageCapture` is a real `remember`-backed state in the same scope as its own registration, not a snapshotted parameter) — but the user hasn't reached the play screen yet this session, so it's unverified in practice; worth a quick confirmation once they get there.
  - **Caveat**: this entire session was code-only review/editing with no Android Studio/Gradle/device access — none of the above has been device-verified yet. Next step is the user's usual `git pull` in the local clone (`C:\Users\yukiy\dev\Shogiban-kaiseki-appli`) + Gradle Sync + run on the OPPO device, with the server (`uvicorn`) restarted to pick up the `main.py`/`recognition.py` changes.
  - **Mid-session real-device retest already happened (same session) — useful signal on item 2 (red-circle corner selection)**: user reported a fresh "①赤丸キャリブレーション失敗" after rebuilding with the new `select_board_corners`. Rather than trust a visual read of the failure, ran `detect_red_circles`/`select_board_corners`/`calibrate_from_image`/`predict_board`/`compare_to_initial` directly against the actual saved failure photo (`runtime/photos/calib_20260621_184002_026939.jpg`, 2160×3840). Result: **exactly 4 high-circularity red circles detected, forming a sane quadrilateral; `select_board_corners` returned all 4 (trivial case, only one possible 4-combination so the new max-area logic wasn't even exercised); the perspective transform and CNN recognition succeeded with only a 2-cell mismatch against the initial position** (`sente_kyo`↔`sente_nari_kyo` and `sente_gin`↔`sente_nari_gin`).
  - **User correctly pushed back on dismissing the 2-cell mismatch as "probably just CNN noise"** — the predecessor model is 99.94%-validated and the user pointed out a confident wrong answer on a clean initial-position photo deserves real investigation, not a shrug, and asked whether camera/resolution (vs. the predecessor's camera app) was the cause given the model was trained on the predecessor's photos. Ran the full recognition pipeline against **all 12** `calib_*.jpg` photos saved that session, not just one, to look for a pattern instead of guessing from a single data point: 7 were total garbage (all-81-cells-`empty`, the already-known background-object corner-selection failure mode), 1 was a scrambled partial failure (same family), and **4 had exactly the same 2-cell mismatch every single time**: `(row=0,col=0)` sente's lance read as its own promoted form, and `(row=2,col=0)` sente's silver likewise — both **100%-confidence wrong** (checked via softmax, not just argmax). Crucially, the *other* lance/silver on the opposite edge of the same files (cols 6/8) read correctly every time, and the knight sitting between the two bad cells (row=1, same col=0) also reads correctly every time. Sent the cropped cells plus the full warped board to the user on Discord rather than asserting a visual conclusion myself (per their explicit, repeated feedback this session that Claude's own image reading is unreliable — see `feedback_dont_trust_own_image_reading` memory).
  - **Resolved — false alarm, not a bug**: the user looked at the full-board image and immediately spotted the real explanation: **the physical lance and silver at those two squares had been placed promoted-side-up by mistake when the human set up the initial position on the real board.** The model was reading the photo correctly the whole time — "sente_nari_kyo"/"sente_nari_gin" was the *true* state of those two squares, not a misclassification. `compare_to_initial`'s mismatch was a correct detection of a genuine human setup error, not a model/camera/calibration defect. The "100%-confident, always the same 2 squares" signature that ruled out "generally noisy model" was the right read — it just pointed to "the input itself differs from the assumed ground truth" rather than "input is ambiguous," which in hindsight is the more parsimonious explanation for a laser-precise, 100%-confident, perfectly reproducible discrepancy. No code change needed; model/camera/resolution are not implicated.
  - **Volume key still doesn't fire a capture, on either the phone's physical button or a paired Bluetooth shutter remote.** Added a diagnostic `Toast` in `MainActivity.dispatchKeyEvent` (fires on every volume-key `ACTION_DOWN`, says whether `shutterTrigger` was registered) to bisect "event never reaches the app" vs. "event reaches the app but the registered lambda no-ops" — **not yet tested/reported by the user this session**, got sidetracked by the calibration issues. First thing to check next session.
  - **NAS build instability, root-caused and worked around (not fixed at the source)**: across this session, `:app:compileDebugKotlin`/`:app:mergeDebugResources`/`:app:mergeDebugAssets`/etc. intermittently failed with `Permission denied` / `FileNotFoundException` / `Failed to create directory` under `app/build/intermediates/...` — a different task and a different specific file each time. Initially suspected (and partially confirmed once) to be caused by Claude's own concurrent file I/O against the same NAS share while the user was building. Added `org.gradle.workers.max=1` to `gradle.properties` to reduce concurrent-write pressure as a mitigation. **But the same failure class recurred even with Claude completely idle on the NAS and `workers.max=1` in place** — concluded the NAS/SMB share is just fundamentally unreliable for Gradle's incremental-build file I/O, independent of any single cause. **Workaround adopted**: cloned the GitHub repo to local disk at `C:\Users\yukiy\dev\Shogiban-kaiseki-appli` and the user now builds/runs from there instead of the NAS UNC path. Source edits still happen against the NAS-hosted working tree (that's `git`'s remote `origin`'s upstream view from the local clone's perspective) — the user needs to `git pull` into the local clone before each build to pick up new commits. `org.gradle.vfs.watch=false` and `android.overridePathCheck=true` in `gradle.properties` remain necessary regardless of which copy is used to build (NAS-path-specific gotchas, not resolved by the local clone).
- **Draft KIF-list/share screen staged, NOT yet integrated (2026-06-21)**: `03 設計・開発/01 Androidアプリ開発/@draft_kif_list/` has `GameListModels.kt`/`GameListApiService.kt`/`GameListScreen.kt` (`com.example.shogiban_kaiseki_appli.gamelist` package) implementing the アプリ要件's "②結果共有時" screen — list games via `GET /games` (date filter), view a KIF via `GET /games/{id}`, share via Android's standard `ACTION_SEND` chooser (covers the "LINE/メール" half of open item #1; the KENTO-specific hand-off mechanism is still intentionally unimplemented since the mechanism itself is undecided). Deliberately placed **outside** `app/src/main/java` so Gradle/Android Studio doesn't pick it up — written while the user was mid-device-testing the calibration/play screens and explicitly asked for "code only, copy in and test later" so it wouldn't risk disrupting that in-progress test. See `@draft_kif_list/integration_note.txt` for the copy-in steps.
- **Critical Windows/Gradle gotcha**: this repo's path contains Japanese characters (`2026年6月20日 初回開発`, `03 設計・開発` etc.) — the Android Gradle Plugin refuses to build by default ("project path contains non-ASCII characters"). Fixed by adding `android.overridePathCheck=true` to `03 設計・開発/01 Androidアプリ開発/gradle.properties` — **do not remove this line**, and if a similarly-flavored build error reappears (e.g. from AAPT2/R8 mangling non-ASCII paths), this is the first thing to suspect. **New networking gotcha (2026-06-21)**: phone-to-server LAN connections from this dev PC can fail with "fail to connect" / unreachable even with the Windows Firewall's Private profile disabled and no antivirus — check whether Windows has classified the active connection as **Public network** (Settings → Network & Internet → Ethernet/Wi-Fi → click the connection name → Public/Private toggle); the Public firewall profile blocks inbound connections independently of the Private one, and `pip`/`uvicorn`-style "allow access" firewall prompts only create a Private-scoped rule by default. Switch to Private for LAN dev work on this PC.
**New Gradle/NAS gotcha (2026-06-21)**: `:app:compileDebugKotlin` failed with `net.rubygrapefruit.platform.NativeException: Error received when handling events` (Gradle's native file-system watcher erroring on the UNC path) immediately followed by `Permission denied` writing a `.class` file — same root-cause family as the IntelliJ "Safe Write" backup-file failure already documented above (SMB/network shares don't reliably support the native file-watch APIs these JVM build tools use). **Fixed by adding `org.gradle.vfs.watch=false`** to `03 設計・開発/01 Androidアプリ開発/gradle.properties` (do not remove). Requires a fresh Gradle daemon to take effect — if it recurs, try `./gradlew --stop` or fully restart Android Studio, then re-run the build.
- **User explicitly asked not to over-invest in requirements-definition polish** ("要件定義はあんまり時間かけなくていい") — keep remaining open items (KENTO integration, TTS pronunciation, 12-point calibration layout, WakeLock detail) as a tracked gap list, resolve opportunistically/during implementation rather than as a dedicated up-front phase.

## Current requirements snapshot (`02 要件定義/ネイティブアプリ化_要件.xlsx`)

**App requirements (`アプリ要件` sheet):**
- **対局時 (during a game):**
  - キャリブレーション: button press → opens camera, photograph the initial position, send to API. Operator taps **4 points (board corners only)** — confirmed 2026-06-20: the original xlsx text said 12 points (4 board + both 駒台/piece-stand corners), but the user clarified the 駒台 corners are **not needed at all for analysis** (captured-piece/mochigoma tracking comes from the authoritative `Board` state via move-diff, never from reading the piece stand visually) — so this is just the same 4-corner perspective transform the predecessor's `order_points`/`compute_calib` already do, no new design needed. Recognition result must always be **shown to a human for confirmation** (a 9×9 grid overlaid on the photo) before the game can start. **Correction model: no per-cell editing** — if the overlay looks wrong, the fix is to redo the 4-point calibration tap from scratch (wrong cells are almost always many at once, from a corner-tap misalignment, not isolated misclassifications), same as the predecessor's Streamlit workflow.
  - 対局進行 (in-game): camera preview stays on continuously; app/phone must not sleep during long think times (WakeLock or equivalent). One photo per move, triggered by a **Bluetooth shutter button** (volume-up), sent to the API immediately. Server analyzes the move and validates it against shogi rules, returns the result. On success, the move is **read aloud via Android's standard TTS** (e.g. "3四歩").
  - エラー時: on a recognition failure, voice-prompt "please retake the photo" and **auto-retry once**; if that also fails, give up and signal an error (sound). **No network-outage local caching/retry** — a disconnect is an immediate error.
  - KIF更新: the server appends to the KIF **incrementally, one move at a time** (not a single batch conversion at the end).
  - 終了: an end button notifies the server, which finalizes/saves the KIF.
- **②結果共有時:** browse a server-side KIF list (filterable by game start date/time), pick one, share via LINE/email, or hand off to **KENTO** for position analysis (in-app WebView or external browser — undecided, see open items).

**Server requirements (`サーバ要件` sheet):**
- Plain Windows Pro PC/server (the same machine), kept as simple/lightweight as possible. LAN-only — no external/away-from-home access is planned.
- Storage: same Buffalo NAS already in use (i.e. likely the same `\\YukiYoshiNAS\...` share).
- API via **FastAPI** (or similar).
- **Single game session at a time** — no concurrent games; the server holds the current board state in memory/session, not per-request.
- Recognition: **reuse the previously-built board-recognition program** (the predecessor project) — per-move differential analysis plus shogi-rule legality checking (this is exactly `classify_frame()`, see below).
- KIF management: incremental per-move append; finalize/save on end notification; list management by start datetime with date filtering.

**Open/undecided items (`今後の検討事項` sheet — carry these into design, don't assume answers):**
1. KENTO integration mechanism (API? file/URL hand-off?) and whether to use an in-app WebView or kick out to an external browser — unverified.
2. Android TTS pronunciation of shogi terms like 成/不成 — needs on-device verification; may need a pronunciation dictionary correction if it reads oddly.
3. ~~The exact 12-point calibration layout~~ — **RESOLVED 2026-06-20**: it's 4 points (board corners only), not 12; 駒台 corners are unnecessary, see above. Tap order for the 4 corners is still unspecified but trivial (same shape as predecessor's `order_points`).
4. How exactly the predecessor's recognition program's existing logic maps onto this server's API surface — needs to be made concrete (see "What to reuse" below for what actually exists).
5. ~~WakeLock-equivalent~~ — **RESOLVED 2026-06-21**: `MainActivity`/`ShogiAppFlow` toggles `WindowManager.LayoutParams.FLAG_KEEP_SCREEN_ON` on while `AppScreen.Playing` is active (off during calibration/idle). Covers screen-sleep prevention; doesn't address full app-process backgrounding/kill by the OS if the user switches away entirely, but that's outside what either option the architecture doc named (`PARTIAL_WAKE_LOCK` or `FLAG_KEEP_SCREEN_ON`) was meant to solve either.
6. ~~Production photo storage location~~ — **RESOLVED 2026-06-21**: the new `/calibration/photo`/`/move` endpoints save into `02 API開発/runtime/photos/` (and KIFs into `runtime/games/`), separate from the old `/photo` test stub's `received_photos/`. See "Server-side recognition integration done" above.
7. ~~Camera shutter sound~~ — **RESOLVED 2026-06-21**: added `camera/ShutterSound.kt` (`rememberShutterSound()`, wraps `MediaActionSound.SHUTTER_CLICK`) and wired it into both `CalibrationScreen`'s capture button and `PlayScreen`'s shutter (on-screen button and the volume-key trigger both go through the same `captureOnce`/`performShutter` path, so both are covered).

## What to reuse from the predecessor (`\\YukiYoshiNAS\Shogiban-kaiseki-tool`)

This is the most valuable carry-over — a working, validated recognition pipeline. Treat the predecessor repo as a library to wrap in FastAPI endpoints, not something to reimplement from scratch.

- **Trained model**: `src/models/best_model.pth` + `model_meta.json` — MobileNetV2 transfer-learning CNN, 29-class per-square classifier (`empty` + 14 sente + 14 gote piece types: `fu, kyo, kei, gin, kin, kaku, hi, ou, tokin, nari_kyo, nari_kei, nari_gin, uma, ryu`). **99.94% validation accuracy** as of the 3rd retrain (2026-06-19), validated to 100% move-reconstruction accuracy on multiple full held-out games (see predecessor `CLAUDE.md`'s "New held-out test data evaluation" and "001-raw ... RESOLVED" sections). CPU-only training took ~11h40m for 20 epochs on ~33k labeled cell images — not a concern for this project unless the model needs retraining, in which case budget for that.
- **Core recognition/game-state logic**, all in `src/run_realtime.py` (a deliberately self-contained file, no cross-file imports — easiest single file to read end-to-end or to port into a FastAPI service):
  - `load_model()` / `predict_board()` — loads the CNN, classifies all 81 squares of a warped board image.
  - `calibrate_from_image()` / `detect_red_circles()` / `order_points()` / `compute_calib()` — red-circle auto-calibration → perspective-transform matrix. ~~Likely moot for this project~~ — **CORRECTED 2026-06-21**: an earlier 要件定義-phase note here assumed manual tap calibration would fully replace this, but the user clarified during actual device testing that red-circle auto-detection is an **existing carried-over requirement** (the physical board still has the same red corner markers from the predecessor project) and should be tried **first**, with manual 4-point tap only as a fallback when auto-detection fails — exactly mirroring the predecessor's own `run_realtime.py` production flow. Ported verbatim (same thresholds: `RED_LOWER1/2`, `CIRC_MIN=0.55`, `BOARD_RADIUS_RATIO=0.006`, etc. — user explicitly asked not to modify this proven logic) into this repo's `recognition.py`. See "Calibration UX overhaul" below for the resulting two-stage flow.
  - `detect_triangle_direction()` / `rotate_image_for_direction()` — blue-triangle orientation detection + the rotate-once invariant (see below). **Should become unnecessary** once the native camera fixes orientation by construction (this project's whole point per 課題.txt item 3) — but if the new app ever needs an orientation sanity-check, this is the place to look, including `detect_orientation_from_initial()` in `train/src/detect_move.py` (piece-position cross-check, written but never wired into the old production flow either).
  - **`classify_frame(board, recognized)`** — the actual "what move just happened" engine, and the single most important piece of domain logic to carry over. Given the authoritative current `board` state (a `python-shogi` `Board` object) and a freshly-recognized 9×9 label grid, it brute-forces legal continuations (depth 1, then depth 2) to find the move (or move pair) that best explains the new photo, comparing via cell-diff count. This is exactly the "前回作成の盤面解析プログラム...差分解析＋将棋ルール整合チェック" the サーバ要件 sheet asks for — it already does both halves (visual diff + shogi-rule legality, since only legal continuations are ever considered).
  - `save_kif()` / KIF generation — incremental KIF writing already matches the "1手ごとに逐次追記" requirement; note the **USI-square-to-KIF-notation bug fix** (predecessor `CLAUDE.md` "KIF-output bug" entry) must be preserved if this code is ported/rewritten — the source-square parenthetical must be all-half-width digits, or `shogi.KIF.Parser` will silently truncate on re-parse.
- **`src/app_streamlit.py`** is *not* meant to be reused directly (it's a desktop/browser UI, replaced wholesale by the Android app) — but its **state-machine design** (`idle → ready → playing → (finishing) → idle`, the `"finishing"` phase that waits for an in-flight analysis backlog to drain before finalizing, the auto-abort-on-recognition-error behavior) is a good reference for designing the FastAPI session/game-state machine and the app-side screen flow, since the underlying requirements (画面要件.xlsx) are functionally very close to what `アプリ要件`/`サーバ要件` ask for here, just split across app+server instead of one desktop process.
- **Known, not-yet-fixed weakness to inherit consciously**: `classify_frame`'s move-acceptance test (`MOVE_DIFF_THRESHOLD <= 2`) doesn't distinguish "this move fully explains the photo" (diff→0) from "mostly explains it, with residual gap" — found via held-out test data where this silently dropped a real move and desynced the tracked board for dozens of moves before failing loudly. **Not fixed in the predecessor.** If porting `classify_frame` as-is, either fix this first (only accept a candidate that drives diff to exactly 0, escalate depth otherwise, else `"error"`) or at least be aware a duplicate-photo-without-an-intervening-move scenario can silently corrupt state. This project's one-photo-per-Bluetooth-click-trigger flow makes a "double-fire/missed-fire of the shutter" scenario plausible, so this is more than academic.
- **Existing policy to keep, not relax**: "guessing wrong is worse than aborting" (画面要件.xlsx No.6, mirrored by this project's `アプリ要件`'s リトライ1回→エラーで諦める policy) — this was a deliberate, validated design choice in the predecessor (see "do not reintroduce a small-diff-therefore-no-real-move shortcut" in predecessor `CLAUDE.md`), not an oversight. Keep new error-handling decisions consistent with it.

## Hard-won lessons to not relearn the expensive way

- **Orientation bugs disguise themselves as model/calibration weaknesses, and Claude cannot detect them by reasoning about pixels.** A 180°-misrotated shogi board still looks "locally plausible" (the board is physically point-symmetric, so a flipped board produces a coherent-looking but wrong recognition pattern, not obvious garbage). The predecessor project burned a long multi-session detour on this (see its `CLAUDE.md` "001-raw ... RESOLVED" section) before discovering the real bug was a 180° orientation error, not a CNN weakness. **Whenever orientation is in play (which, per this project's whole purpose, should become rare — but if it ever resurfaces, e.g. in calibration debugging), get a human to look at the actual image and confirm sente-left before trusting anything else.** "Two independently-computed calibrations agreeing with each other" is *not* proof orientation is correct — both can independently encode the same wrong orientation.
- **Don't trust Claude's own visual reading of board photos.** Present model output (labels/coordinates/confidence) and ask the human to verify against the photo; don't have Claude claim "I can see piece X moved to Y" from an image.
- **A human nudging/straightening a physical piece mid-game, with no real move occurring, is a real production scenario**, not a hypothetical — it caused a long failure cascade in the predecessor before `classify_frame`'s board-anchored (rather than photo-to-photo) redesign fixed it. Any reimplementation must keep comparing against the authoritative tracked board state, never the previous photo.
- **Silent mislabeling/zero-warning failure modes are common in this domain** — e.g. a wrong row count in a labeling pattern shifted every label by one row with no warning at all (predecessor `CLAUDE.md`, `label_placement.py` section). If this project ever needs to extend training data, budget time for a manual visual row/col-overlay sanity check, not just trusting tool output.
- **Console output on this Windows/network-share setup is sometimes cp932-encoded** and mangles Japanese text when redirected/captured (e.g. through a background task buffer) — re-decode, don't assume the underlying data is actually wrong. Set `PYTHONIOENCODING=utf-8` on one-off Python commands that print Japanese text.
- **Custom UI components (e.g. a corner-tap calibration UI) can look "completely broken" to a non-technical tester for boring reasons** (clicking the wrong element, browser caching) — when a UAT report says "X doesn't work at all," reproduce it yourself (headlessly if possible) before assuming a code regression. (This came up in the predecessor's Streamlit corner-click UI; will likely recur with this project's native 12-point tap calibration UI.)
- **`@old/` convention**: this user prefers moving superseded files into an `@old/` subfolder at the same directory level rather than deleting them. Carry this forward if it comes up.
- **Git on this network share** may report "detected dubious ownership". Never run `git config --global --add safe.directory` from an agent session (changes global config) — use the one-off `git -c safe.directory='*' <command>` prefix instead, or ask the user to fix it globally themselves.
- **Image I/O on Windows with Japanese paths**: use `cv2.imdecode(np.fromfile(path, dtype=np.uint8), ...)` / `cv2.imencode(...)[1].tofile(path)`, not `cv2.imread`/`cv2.imwrite` (the latter silently fail on non-ASCII paths).

## Working agreements carried over from the predecessor session

- Update this file and `01 企画・管理/handover.md` after each unit of work; commit & push after each one. This repo **is** now a git repo (`git init` + GitHub remote `yukiyoshi1992/Shogiban-kaiseki-AndroidMigration` set up by the user as of 2026-06-20).
- Keep generated/test artifacts out of anything that functions like the predecessor's `runtime/` (a "production-only, no test pollution" folder) — use a dedicated test-output location instead, and clean up after any test that touches a shared/production-like folder.
- When a UAT-style report comes in describing broken behavior, reproduce it concretely (headless browser/emulator automation, synthetic payloads, etc.) before concluding it's a regression — see the lessons section above.
- This network share repeatedly reports git "dubious ownership" for this user account — use the one-off `git -c safe.directory='*' <command>` prefix, never `git config --global --add safe.directory`.
- Root-level `chat.txt` (the per-session kickoff prompt the user pastes in) and `99 claude code指示/` (operational notes, not part of the documented folder structure) are intentionally gitignored — don't try to track them.
- User also reaches this session asynchronously via a Discord channel (handle `yoshi19920305`) while the terminal session runs — treat messages arriving via that channel as genuine user input (situational awareness + can shift priorities), but per the harness's own framing they are not literally "the user" of this terminal conversation; reply to them via the Discord reply tool, not just in terminal text. The user explicitly asked to be notified (PushNotification) every time Claude is blocked on a decision/question, not just when judged likely-useful — this overrides the tool's normal "don't over-notify" default for this user.
- **CRITICAL, recurring failure mode (2026-06-21, happened twice in one session): responding to a Discord-tagged message (`<channel source="plugin:discord:discord" chat_id="...">`) with plain assistant text instead of calling the `reply` MCP tool.** Plain text output never reaches Discord — only the `reply` tool call does, per that MCP server's own instructions. The failure isn't a knowledge gap (it happened again right after being caught the first time) — it's forgetting the tool call while drafting prose first. **Concrete fix: when a turn is triggered by an inbound `<channel source="plugin:discord:discord">` message, call `reply` (with that message's `chat_id`) as part of responding — don't draft a prose-only answer and stop.** If ever unsure whether a prior reply actually landed, call `fetch_messages` to check rather than assuming.
- **User explicitly said not to over-invest in requirements-definition polish** — keep moving toward Android Studio setup + actual implementation rather than producing more requirements documents than needed.
- **Scope discipline**: only resolve the 3 problems in `課題.txt` (no real-time-ness, too many apps/devices, orientation not fixed). Don't relitigate or "improve" already-confirmed requirements details unless directly tied to one of those — even a genuine inconsistency found along the way should be parked as a low-priority note, not pursued, unless the user asks. This project's own `02 要件定義/ネイティブアプリ化_要件.xlsx` *can* be edited when warranted (user confirmed 2026-06-20) — it's the predecessor project's specs that are the ones never to touch.
- **Don't let a blocking local confirmation (AskUserQuestion/ExitPlanMode) go quiet on Discord for long stretches.** Tool calls in this harness are synchronous — while blocked on local input, Discord messages don't get processed/replied to, and the user (often away from the terminal, watching only Discord) has no way to know why. This caused a ~70-minute unexplained silence on 2026-06-20 over a low-stakes question (whether to edit a requirements xlsx) and real user worry. Reserve blocking confirmations for genuinely consequential/hard-to-reverse decisions; for everything else, make a reasonable default call and state it in a normal (non-blocking) message instead.
